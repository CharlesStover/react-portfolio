import withStyles from '@material-ui/core/styles/withStyles';

export default withStyles({
  root: {
    alignItems: 'flex-end',
    display: 'inline-flex',
    fontSize: '0.75rem',
    height: '1em'
  }
});
